version: '3.7'

services:
    api:
        container_name: api
        build:
            dockerfile: Dockerfile
            context: .
        volumes:
            - "..:/app"
        ports:
            - "49159:5000"